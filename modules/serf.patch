diff --git a/CMakeLists.txt b/CMakeLists.txt
index babcaba..28b05d3 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -40,10 +40,10 @@ set(CMAKE_MODULE_PATH "${SERF_SOURCE_DIR}/build")
 include(SerfVersion)
 
 project("Serf" VERSION ${SERF_VERSION} LANGUAGES C)
-message(WARNING
-        "Serf's CMake build is considered EXPERIMENTAL. "
-        "Some features are not supported and the build "
-        "has not been tested on many supported platforms.")
+# message(WARNING
+#        "Serf's CMake build is considered EXPERIMENTAL. "
+#        "Some features are not supported and the build "
+#        "has not been tested on many supported platforms.")
 
 
 # Build options
diff --git a/buckets/ssl_buckets.c b/buckets/ssl_buckets.c
index 1b28560..f95292f 100644
--- a/buckets/ssl_buckets.c
+++ b/buckets/ssl_buckets.c
@@ -1654,8 +1654,7 @@ static int ssl_need_client_cert(SSL *ssl, X509 **cert, EVP_PKEY **pkey)
             }
             else {
                 serf__log(LOGLVL_ERROR, LOGCOMP_SSL, __FILE__, ctx->config,
-                          "OpenSSL cert error: %d %d %d\n", ERR_GET_LIB(err),
-                          ERR_GET_FUNC(err),
+                          "OpenSSL cert error: %d %d\n", ERR_GET_LIB(err),
                           ERR_GET_REASON(err));
                 PKCS12_free(p12);
                 bio_meth_free(biom);
